let add = fn(a, b) {
    return a + b
}

let result = add(5, 3)
print("5 + 3 = " + str(result))

let factorial = fn(n) {
    if n <= 1 {
        return 1
    }
    return n * factorial(n - 1)
}

print("factorial of 5 is " + str(factorial(5)))
